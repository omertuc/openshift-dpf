# @docs-as-code: start section "nfd-cr"
#   | param: "api.<CLUSTER_FQDN>"
#   | param: "name: nfd"
apiVersion: nfd.openshift.io/v1
kind: NodeFeatureDiscovery
metadata:
  name: nfd
  namespace: openshift-nfd
spec:
  operand:
    workerEnvs:
      - name: KUBERNETES_SERVICE_HOST
        value: api.<CLUSTER_FQDN>
      - name: KUBERNETES_SERVICE_PORT
        value: "6443"
  workerConfig:
    configData: |
      sources:
        pci:
          deviceClassWhitelist:
            - "0200"
            - "03"
            - "12"
            - "0207"
          deviceLabelFields:
            - "vendor"
            - "device"
            - "class"
# @docs-as-code: end section "nfd-cr"

---
# @docs-as-code: start section "nfd-rule"
apiVersion: nfd.openshift.io/v1alpha1
kind: NodeFeatureRule
metadata:
  name: dpu-detection-rule
  namespace: openshift-nfd
spec:
  rules:
    - name: "DPU-detection-rule"
      labels:
        "dpu-enabled": ""
      matchFeatures:
        - feature: pci.device
          matchExpressions:
            vendor: {op: In, value: ["15b3"]}
            # TODO: Make this settable from the values
            device: {op: In, value:  ["a2d6", "a2dc"] }
# @docs-as-code: end section "nfd-rule"
